commit-msg:
  scripts:
    "msg.sh":
      runner: bash
pre-push:
  parallel: true
  commands:
    build:
      run: go build -o build/app cmd/main.go
    coverage:
      run: go test -coverprofile=coverage.out ./...
